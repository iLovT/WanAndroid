<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 实心 -->
    <solid android:color="@color/bg_not_select_color" />
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/bg_not_select_color" />
    <!-- 圆角 -->
    <corners android:radius="15dp" />
    <!-- 边距 -->
    <padding
        android:bottom="5dp"
        android:left="5dp"
        android:right="5dp"
        android:top="5dp" />
</shape>